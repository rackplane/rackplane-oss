# RackPlane OSS Edition Requirements
# AGPL-3.0 Licensed - Open Source Build
# Excludes: Cloud OCR, Stripe, AI/LLM, Advanced Web Scraping

# Core Web Framework
fastapi==0.109.1  # Security fix: CVE-2024-24762 (ReDoS)
uvicorn==0.27.0
pydantic==2.6.0
pydantic-settings==2.1.0
email-validator>=2.0.0
python-multipart==0.0.7  # Security fix: CVE-2024-24762 (ReDoS)

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4  # TODO: Consider migration - CVE-2024-52304 (insecure MD5)
bcrypt==4.1.3

# Reporting & Export
reportlab==4.0.9
openpyxl==3.1.2
pandas==2.2.0
pypdf2>=3.0.0  # PDF parsing for invoice import

# Image Processing (Local Only)
Pillow>=10.0.0  # Version 10 for better compatibility

pytesseract==0.3.10  # Local OCR using Tesseract (free, works offline)
imagehash==4.3.1  # Perceptual hashing for duplicate image detection

# HTTP Clients
requests==2.32.0  # Security fix: CVE-2024-35195, CVE-2025-23327, CVE-2025-23328
httpx==0.26.0  # For basic API integration

# Web Scraping (Basic - HTML only, no JavaScript support)
beautifulsoup4==4.12.3  # For basic web scraping

# Label Printing - EXCLUDED FROM OSS
# Reason: labelprinterkit requires Pillow<10, which conflicts with Pillow>=10 that OSS uses
# See premium edition for label printing support with Pillow 9.x
# labelprinterkit==0.7.1
# pysnmp==4.4.12
# pyasn1==0.4.8
# pyasn1-modules==0.2.8

# Background Tasks
celery==5.4.0  # Security fix: CVE-2024-56324 (command execution)
redis==5.0.1

# NetBox Integration (Read-Only in OSS)
pynetbox==7.3.3

# Email
sendgrid==6.11.0
jinja2==3.1.6  # Security fix: Multiple CVEs (XSS, code execution)

# Object Storage
minio==7.2.3  # MinIO client for object storage (photos, documents)

# Configuration
# anthropic - Excluded from OSS (AI/LLM premium feature)
PyYAML==6.0.1  # For demo tenant seeding from YAML fixtures

# Observability & Resilience
prometheus-fastapi-instrumentator==6.1.0
pybreaker==1.2.0
aiocache==0.12.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-html==4.1.1
pytest-xdist==3.5.0
pytest-mock==3.12.0

# ============================================================================
# EXCLUDED FROM OSS BUILD (Available in Premium Edition)
# ============================================================================
# google-cloud-vision - Cloud OCR (Google Vision API)
# anthropic - AI/LLM features (Claude API)
# stripe - Premium billing integration
# playwright - Advanced web scraping with JavaScript support
# boto3 - AWS Textract Cloud OCR
# azure-cognitiveservices-vision-computervision - Azure Computer Vision
#
# For premium features, see: https://rackplane.com/pricing
# ============================================================================
